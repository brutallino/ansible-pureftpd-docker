galaxy_info:
  author: Derek Merck
  description: Configure and run a Pure-FTPd server in a Docker container.

  role_name: pureftpd-docker

  license: MIT

  min_ansible_version: 2.4

  galaxy_tags:
  - docker
  - ftpd
  - pureftpd

dependencies:
  - role: geerlingguy.pip
    pip_install_packages:
      - docker
      - pexpect
    tags: deps
  - role: geerlingguy.docker
    tags: deps