galaxy_info:
  author: Derek Merck
  description: Configure and run a [Pure-FTPd](https://www.pureftpd.org/project/pure-ftpd) server in a Docker container.

  role_name: pureftpd-docker

  license: MIT

  min_ansible_version: 2.4

  galaxy_tags:
  - docker
  - ftpd
  - pure-ftpd

dependencies:
  - role: geerlingguy.pip
    pip_install_packages:
      - docker
      - pexpect
  - role: geerlingguy.docker